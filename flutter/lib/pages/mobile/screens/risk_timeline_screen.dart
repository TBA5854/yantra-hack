import 'package:flutter/material.dart';

class RiskTimelineScreen extends StatelessWidget {
  const RiskTimelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Risk Timeline - Mobile')));
  }
}
