import 'package:flutter/material.dart';

class OnChainLogPage extends StatelessWidget {
  const OnChainLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('On-Chain Log - Web')));
  }
}
