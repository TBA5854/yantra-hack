import 'package:flutter/material.dart';

class StressBreakdownPage extends StatelessWidget {
  const StressBreakdownPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Stress Breakdown - Web')));
  }
}
